<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_graph"
    app:startDestination="@id/homeFrag">

    <fragment
        android:id="@+id/homeFrag"
        android:name="br.com.app5m.boxifarma.ui.fragment.HomeFrag"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFrag_to_consultPounchFrag"
            app:destination="@id/consultPounchFrag" />
    </fragment>
    <fragment
        android:id="@+id/consultPounchFrag"
        android:name="br.com.app5m.boxifarma.ui.fragment.ConsultPounchFrag"
        android:label="fragment_consult_pounch"
        tools:layout="@layout/fragment_consult_pounch" />
</navigation>